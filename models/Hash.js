// const mongoose = require("mongoose");
// const { Schema } = mongoose;

// const userSchema = new Schema({
//   hash: { type: String, default: uuidv4 },
//   _user: { type: Schema.Types.ObjectId, ref: "User" },
// });

// mongoose.model("User", userSchema);
